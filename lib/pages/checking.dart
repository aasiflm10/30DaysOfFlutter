import 'package:flutter/material.dart';

class Checking extends StatelessWidget {
  const Checking({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("terminal commit"),
      ),
    );
  }
}
